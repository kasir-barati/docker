1. Use an env variable for book-api port! Currently hardcoded (3000).
2. What the hell is `init-zitadel-pat-vol` service doing for me?
3. Are `ZITADEL_DATABASE_POSTGRES_USER_USERNAME` and `ZITADEL_DATABASE_POSTGRES_USER_PASSWORD` necessary? Where we are even creating them?