FROM mongo:4 as dump

ARG DUMP_MONGODB_URI
ARG MONGODB_DATABASE
ARG RESTORE_MONGODB_URI

RUN mongodump --uri ${DUMP_MONGODB_URI}

RUN mongorestore --uri ${RESTORE_MONGODB_URI} -d ${MONGODB_DATABASE} /dump/${MONGODB_DATABASE}
